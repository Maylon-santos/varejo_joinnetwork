import { chromium } from 'playwright';
const browser=await chromium.launch({channel:'chrome',headless:true});
try {
const page=await browser.newPage({viewport:{width:1440,height:1000}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto(new URL('./index.html',import.meta.url).href);await page.screenshot({path:'/tmp/planejamento-desktop.png',fullPage:true});await page.selectOption('#cliente','9');await page.selectOption('#modelo','sub');if(await page.locator('#url').textContent()!=='marca-j.joinnetwork.tech')throw Error('Simulador incorreto');await page.selectOption('#modelo','proprio');if(await page.locator('#url').textContent()!=='painel.marca-j-exemplo.com.br')throw Error('Domínio próprio incorreto');await page.fill('#escolha','varejo.joinnetwork.tech');const downloaded=page.waitForEvent('download');await page.click('#exportar');const download=await downloaded;await download.saveAs('/tmp/decisao-planejamento-test.txt');await page.setViewportSize({width:390,height:844});await page.screenshot({path:'/tmp/planejamento-mobile.png',fullPage:true});const overflow=await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth);console.log(JSON.stringify({errors,overflow,download:download.suggestedFilename()}));if(errors.length||overflow)throw Error('Falha na verificação');
} finally {await browser.close();}
